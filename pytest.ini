[pytest]
testpaths = tests
markers =
    manual: tests requiring agent/WebSearch/human verification (skipped in default CI)
addopts =
    --cov=scripts
    --cov-fail-under=80
    -m "not manual"
    -ra
