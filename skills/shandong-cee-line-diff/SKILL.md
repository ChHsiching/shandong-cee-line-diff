---
name: shandong-cee-line-diff
description: 山东高考录取数据整理——把今年的大绿本招生计划和往年的录取线差数据按专业语义匹配，给每个专业补上线差和标准差。Use when the user provides 山东高考/CEE admission xlsx and wants them matched. Annual reuse; asks data-setup questions first.
---

# 山东省高考录取数据匹配整理

把**今年的招生计划**（大绿本）里每个专业，和**往年的录取数据**按专业名语义匹配，给每个专业补上「往年线差」和「线差标准差」。匹配不上的单独列出，并标注原因。

匹配分两步：**程序严格匹配**（归一化后专业全名完全一致 = 直接匹配）+ **agent 语义匹配**（程序没匹配上的，由 agent 逐条读专业名、理解语义后判断能不能和往年某个专业配上）。agent 匹配完，再做一轮 **agent 二次复核**（逐条验证每个 agent 判断的匹配是否正确），确保结果准确。

---

## 第 0 步：环境（定位 plugin 根 + 确认 venv 就绪）

plugin 根（$P）含 scripts/ + .venv。**Claude Code**：plugin 的 SessionStart hook（`plugin.json` 的 `hooks` 字段 → `hooks/setup-env.sh`）新会话自动建好 `.venv` + openpyxl，agent 不用自己建。**其他工具（Codex/OpenCode/ZCode）/ 万一 hook 没建好**：跑完 Step 0 若 `$P/.venv` 不存在，按下面 fallback 手动建一次（只首次）。

**直接跑这一段**（定位 plugin 根 + 验证环境就绪）：

```bash
# 定位 plugin 根：读每个副本 plugin.json 的 version，挑版本号最大的；
# 只搜 cache/ + 其他 harness 安装目录，排除 marketplaces/（源克隆，不是运行副本）。
# 不按 .venv 选——venv 由下面 fallback 建（首次），避免选到 stale 旧版。
P=""; best=""
for sk in $(find ~/.claude/plugins/cache ~/.zcode/skills ~/.codex ~/.local/share 2>/dev/null -name SKILL.md -path "*shandong-cee-line-diff*"); do
  d=$(dirname "$(dirname "$(dirname "$sk")")")
  [ -e "$d/.orphaned_at" ] && continue  # 跳过已被新版本顶替的 orphaned 副本（fresh-test run8 曾选中 orphaned 1.5.4）
  v=$(grep -m1 -o '"version":[[:space:]]*"[^"]*"' "$d/.claude-plugin/plugin.json" 2>/dev/null | grep -o '[0-9]\+\.[0-9]\+\.[0-9]\+')
  if [ -n "$v" ] && { [ -z "$best" ] || [ "$(printf '%s\n%s\n' "$best" "$v" | sort -V | tail -1)" = "$v" ]; }; then
    best="$v"; P="$d"
  fi
done
# 退化：上面没找到（如只剩 marketplaces 源克隆）再全搜一遍按版本取最大。
if [ -z "$P" ]; then
  for sk in $(find ~/.claude/plugins ~/.zcode/skills ~/.codex ~/.local/share 2>/dev/null -name SKILL.md -path "*shandong-cee-line-diff*"); do
    d=$(dirname "$(dirname "$(dirname "$sk")")")
  [ -e "$d/.orphaned_at" ] && continue  # 跳过已被新版本顶替的 orphaned 副本（fresh-test run8 曾选中 orphaned 1.5.4）
    v=$(grep -m1 -o '"version":[[:space:]]*"[^"]*"' "$d/.claude-plugin/plugin.json" 2>/dev/null | grep -o '[0-9]\+\.[0-9]\+\.[0-9]\+')
    if [ -n "$v" ] && { [ -z "$best" ] || [ "$(printf '%s\n%s\n' "$best" "$v" | sort -V | tail -1)" = "$v" ]; }; then
      best="$v"; P="$d"
    fi
  done
fi
PYTHONPATH=$P "$P/.venv/bin/python" -m scripts.run_pipeline --help >/dev/null 2>&1 && echo "✓ 环境就绪, PLUGIN_ROOT=$P (v$best)"
```

> 为什么读 `plugin.json` 版本而不是按 `.venv` 选：Claude Code 的 cache 里可能同时留多个版本（旧版残留 + 新版），旧版往往已建好 `.venv`、新版还没建——按 `.venv` 选会选到 stale 旧版（实测：1.2.2 有 venv、1.3.0 没有，就错选了 1.2.2，导致一堆已修 bug 被当成新 bug 报）。读 `plugin.json` 的 `version` 取最大，永远选最新装的那份；`.venv` 由 SessionStart hook（Claude Code）或下面 fallback 在那份上建。同时排除 `marketplaces/`（源克隆，不运行），避免和 `cache/` 混。fresh-test run8 后再加一条：**也跳过带 `.orphaned_at` 的副本**（已被新版本顶替、即将清理的旧版——run8 曾选中正被顶替的 1.5.4 跑完整轮）。另：若上面选出的代码版本（`v$best`）和你**这份 SKILL.md 所在路径里的版本号**不一致，是 harness 的 Skill 工具注入了旧版 SKILL.md（代码新版、文档旧版）——**重启会话**让 Skill 工具加载最新版即可，别在新旧不一致的状态下跑。

看到 `✓ 环境就绪` 就直接进第一步。**如果 `$P/.venv` 不存在**，fallback 手动建一次（首次）：
```bash
python3 -m venv "$P/.venv" && "$P/.venv/bin/pip" install -q openpyxl
```

**之后所有 pipeline / audit / show_table 命令用 `PYTHONPATH=$P "$P/.venv/bin/python" -m scripts.<模块>`**（$P 是上面输出的 PLUGIN_ROOT——记住它，后面每条命令都要用）。数据年年变时通过 CLI 参数覆盖（`--dl-file` / `--one-line` / `--supplement-batches` 等），不用改代码。

> **脚本使用约定（低自由度——这是硬约束，不是建议）**：`scripts/` 是**确定性引擎**：严格匹配 / 核心名匹配 / 线差 / 标准差 / 审计都是脆弱、要求每次精确一致的逻辑，所以写成代码让你**直接按上面的命令运行**，而不是用自然语言每次重新实现。因此：**只运行，不修改** plugin 里的这些代码。撞到崩溃或 `Stage2ContractError`（它会把所有问题收齐了一次报全），那是 **skill 的 bug**——**如实把完整错误清单记录下来交开发者**，**不要就地改 plugin 源码绕过**（一改下次 plugin 更新就丢，还会掩盖真正的根因；fresh-test run7 就因为改了缓存源码差点漏判）。允许你调整的只有数据口径，用 CLI 参数（`--dl-file` 等）传；其余按命令原样跑。

---

## 第一步：确认数据 + 自动默认

收到数据文件后（或用户提到山东高考/大绿本/线差），先读文件确认。**绝大多数事项已被内置规则 / 文件自动决定，不要问用户**——只在下面「需要问」那几项不明确时才用 AskUserQuestion。

### 自动决定（agent 直接用，别问）

- **数据路径**：默认 `data/` 下 3 个 xlsx（大绿本 / 近三年线差统计 / 提前批补充表）。文件名能唯一识别就用，不问路径。
- **一段线**：自动从近三年表「说明」sheet 读（`read_one_line_from_notes`）；读不到退到内置默认。**不问。**
- **线差 / 标准差**：近三年主统计表自带，直接用；只有补充表（如提前批）需现场算 `录取最低分 − 一段线`。不问算法。
- **精度**：2 位小数。不问。
- **追加列**：近三年统计线差 / 近三年线差标准差 / 匹配方式 / 仅一年数据 / 选科要求跨年变化 / 二次复核 / 原因说明（7 列，行尾追加、不覆盖原始数据）。不问。
- **未匹配处理**：按基数规则 + 估算路由自动——新专业 / 无有效对应（含多对一、大类）→ 同校同选科均值估算 + 备注，进 `未能匹配的专业.xlsx` 带估算列；飞行 / 表意特别模糊 → 留空。**这是规则，不是用户选项，不要问。**
- **改名**：自动派 agent 网查每一所消失 / 新增校（见 5.2）。用户没主动列已知改名就不问。
- **输出格式**：两版都出——`大绿本_完整版_含线差.xlsx`（保留层级）+ `大绿本_专业列表_含线差.xlsx`（扁平）。不问。

### 需要问（仅当不明确时）

1. **补充表**：除大绿本 + 近三年统计表外，若还有「只有录取分、需现场算线差」的表（0/1/多个，**不一定是提前批**），且从文件结构看不清是哪个批次 / 低分列在哪——问清楚每个：文件名、批次 / 类型、低分列（2025/2024/2023 各第几列，0 开始数）。
2. **整理范围**：默认本科 + 常规批 + 提前批（专科排除）。**仅当**用户明确说了别的范围（如「只要常规批」「含专科」）才按说的来；否则用默认，不问。

用户在调用 prompt 里已经说了的，按说的来，不重复问。**问完（或确认无需问）才开始处理。不猜、不静默跳过。**

问到的文件名 / 一段线 / 补充表配置，跑 `run_pipeline` 时通过 CLI 参数传（`--dl-file` / `--one-line` / `--supplement-batches` / `--supplement-low-cols` 等，见 REFERENCE 的「run_pipeline CLI 参数」段），**不要硬编码到代码里**——数据年年变，参数化才能复用。

**问卷答完，整个整理流程按 REFERENCE 的「管线串联命令」段（8 步）执行**：跑管线 → 派改名 agent → 跑管线（重生成改名感知 batch）→ 派语义 agent → 跑管线 → 派复核 agent → 跑管线 → 审计。SKILL 第二~七步是每步**在做什么**的逻辑说明（你跑管线时这些自动发生），你只需要按 REFERENCE 的命令串联 + 在「派 agent」步骤派 subagent（并发用下面的**自适应并发**规则调，不写死）。

> **自适应并发（AIMD——自动适配你的套餐，不写死 6）**：派 subagent 跑 batch 时，并发按真实反馈调：
> - **起点 6**：第一波并发 6 个（保守起步——fresh-test run9 实测多数套餐并发 ≥8 就易 429，6 安全；干净就 +2 往上探，不是定死 6）。
> - **撞错就减半重试**：某波里有 subagent 撞 **429 / 用量上限 / 超时**这类过载失败 → 把**失败的批**拎出来，下一波并发 **÷2**（最低 2）重派；已成功的批不用重跑。
> - **没撞错就加**：某波全部成功 → 下一波并发 **+2**，**上限 10**（Claude Code 实测的并发天花板，再高一个 turn 也发不出更多 Task 调用）。
> - **兜底**：并发降到 2 还反复 429 → 剩下的批**改在主会话直接处理**（主会话不算 subagent、不受 subagent 并发限）。
>
> 这条天然自纠错：套餐大就自动冲到 10（快）、套餐小就自动降到不触 429（稳）。哪怕你偶尔没按规则调，429 本身也会触发 ÷2，不会失控——所以放心往高了起。

---

## 第二步：读取数据 + 程序严格匹配

### 2.1 读取源文件

- **大绿本**：今年招生计划，按批次/学校/专业分层。专业行 = 代号和名称都非空。
- **往年录取数据**：往年每个学校每个专业的录取分数、线差、标准差。
- **补充录取数据**（**可选**，可能有 0/1/多个，批次不限）：主统计表里没统计到、只有录取分（最低分等）、需要现场算线差的数据。补充表不一定有、不一定几个、**不一定是提前批**——按第一步问清的实际为准；没有就跳过，主统计表已统计的专业直接用现成线差。

每个文件读取前后都做 SHA256 校验，确保字节没被改过（提前批补充表如果不存在就跳过）。专科行（小标题含「专科」）排除。

### 2.2 程序严格匹配

对大绿本的每个专业，取专业名做归一化（统一全角半角、去空白、剥掉体检/语种/学费等不影响专业身份的描述性括号），然后和往年数据里的专业名精确比对。**归一化后完全一致 = 匹配成功**，直接填入往年的线差和标准差。

这一步是确定性的，不需要 agent 介入。大约能匹配 58%。

**完成判据**：每个大绿本本科专业都被处理过（匹配成功或标记为「待 agent 匹配」）。

---

## 第三步：agent 语义匹配

程序严格匹配没配上的专业，交给 agent 逐条处理。

### 怎么做

1. 对每个没配上的专业，找出同一所学校、专业名相似的往年候选（核心名 = 去掉所有括号后的基础专业名，核心名相同的就是候选）。
2. **agent 逐条读**：今年这个专业叫什么、往年候选分别叫什么，**语义判断**是不是同一个专业。
3. 语义相同 → 匹配成功，填入对应候选的线差和标准差。
4. 语义不同 → 标记「无法匹配」。
5. 不确定 → 标记存疑，留到第四步问用户（但应该很少，绝大多数 agent 自己就能判断）。

### agent 怎么判断「同一个专业」

单一判据——**基数规则**（往年数据是基准，今年往往年上靠）：

- **一对一、一对多可以；多对一不行。** 往年某校某核心专业只有 1 个 → 今年这个核心下的**所有变体**都匹配这 1 个（**一对多**：1 个往年 ↔ 多个今年）。往年同核心有多个 → 今年每个专业按实际描述对应到其中**某一个**（**一对一**）。绝不能把往年多个并到今年一个上（**多对一**）——多个往年里没对上的该进「停招」表，不能拿它们的均值凑一个线差给今年。

在这个判据下，差异分两类：

- **永远不算身份的差异**（任何时候都当核心名本身看，对得上就匹配）：
  - **培养模式标签**：拔尖、卓越、创新、英才、基地、未来、试验班、订单班等「XX 班」——只是培养组织方式，不创造新专业。往年「数学」对今年「数学(拔尖)」永远没问题，哪怕往年同核心有多个。
  - **描述性噪音**：标点、词序、体检/身高/色盲、学费、语种、章程引用、加减括号内容、子专业清单的大段描述。今年的专业名通常比往年起得长（大绿本写一大段），往年通常就一个短名——看核心名 + 主要方向就够，多余描述不影响。
- **大类 ↔ 具体是一对多的常见形式**：往年按「经济学类」大类招、今年拆成「经济学」等具体——这是 **1 个往年大类 ↔ 今年具体**，属**一对多**（基数规则允许），配（往年大类线差作参考）。同理 护理学↔护理学类、法学↔法学类。宽大类（「工科试验班类」混杂方向、录取分差大）对不上→走第五步估算，别一刀切。
- **一对多时被吸收、一对一时才算不同专业的差异**：中外合作、师范、性别（男/女）、招生类别（普通/地方专项/综合评价）、真正不同的方向（如「投资学(量化投资)」≠「投资学」）。
  - 往年同核心只 1 个（**一对多**）→ 今年这些变体也都匹配那 1 个（只有这 1 个数据可用）。
  - 往年同核心多个（**一对一**）→ 这些差异就是不同专业了，今年每个专业按描述挑往年**一个**对应的；今年这个专业若往年只有「中外合作」「师范」、今年是「普通」这种对不上的，就别硬配，走第五步估算。

关于学校（匹配的前提是**先同校**）：

- **独立招生的不同校区 = 不同的学校**（华北电力大学(北京) ≠ 华电(保定)）——校名不同，匹配前就按校名分开了，根本不会进同一个候选池。同一学校多校区一起招生的不算不同学校。
- **学校改名/转设/合并** → 上网搜所有消失/新增的学校确认（见 5.2）。
- **校名/校区不确定** → 上网搜招生章程确认。

### 数据量大时

25474 个专业中大约有 5000-6000 个需要 agent 匹配。用 subagent 并行处理（每个 subagent 处理一批，约 20 个专业），可以同时派多个 subagent。**不惜 token，追求准确**：每个专业都要认真读、认真判断，不能偷懒跳过。

**完成判据**：每个待 agent 匹配的专业都有结果（匹配成功 + 对应候选 / 无法匹配 / 存疑）。

---

## 第四步：agent 二次复核

只有 **agent 语义匹配**（agent 判断的）需要复核。**核心名匹配**（程序 Stage 1.5 把「往年同核心只 1 个」的 past=1 直接配上）是构造确定——像严格匹配一样**不进复核**（基数规则已定，agent 保守判断只会添乱）。这一步只复核 agent 语义匹配：另一个 agent 逐条看「这个匹配对不对」，判「确认」或「存疑」。

- 确认 → 留在主表。
- 存疑 → 移到特殊情况表，不进主表。

### 复核时怎么判断

复核 agent 用的就是第三步那条**基数规则**（一对一/一对多可以、多对一不行；培养模式标签永远不算身份；中外合作/师范/性别/类别/学制/独立划线校区/真方向在一对多时被吸收、一对一时才算不同专业）。每条复核任务的 `requirement` 字段会**按「往年同核心几个」自动给出对应那版的规则**（程序已数好往年同核心数），照着判就行：

- 往年同核心只 1 个 → 今年任何校内变体都判「确认」（一对多）。
- 往年同核心多个 → 培养模式标签差异判「确认」；中外合作/师范/招生类别/真方向实质不同判「存疑」。
- 不确定 → 判「存疑」，宁可保守。

**这一步是硬性的**：主表里不能有未经复核的判断型匹配。宁可保守（多移几条到特殊情况），也不能让错配留在主表。

**完成判据**：每个判断型匹配都有复核结果（确认 / 存疑）。

---

## 第五步：处理边界情况

### 5.1 agent 确认匹配不到的专业 → 逐级估算

agent 认真读后**确认**匹配不到往年同一个专业（不是「不确定」，是确认不是同一个）→ 按以下顺序逐级估算线差参考值，标注「估算」（和真实匹配的线差分开，不混淆）：

1. **同校 + 同选科**的其他专业线差均值（最精确）。
2. 同校 + 所有专业线差均值（同选科没有时的退化）。
3. 整所学校往年完全没在山东招过生 → 无法估算，线差和标准差留空，标注「新校无历史数据」。

线差和标准差都保留 2 位小数。标准差 = 所用专业的线差标准差的均值（不重新算总体标准差）。

注意：去年今年虽然改了名字/方向但本质是同一个专业的（如「临床医学(5+3一体化儿科学)」vs「临床医学(5+3一体化,儿科学)」逗号差异）→ 这是匹配成功，不进估算。

**「对不上」的也估算（不留空）**：同核心名往年有、但构成**多对一**（如今年 1 个大类 ↔ 往年多个具体专业）或类别/方向全对不上、确认找不到有效对应的——和真新增一样按上面三级估算，结果写进 `未能匹配的专业.xlsx` 的估算列 + 原因说明写清楚「对不上，按同校同选科均值估算」。只有**表意特别模糊**、连选科都看不出门道的，才留空。

### 5.2 改名/转设/合并的学校 → 网查 + 联动

往年有、今年消失的学校 × 今年有、往年没有的学校 → 可能是改名了。

**网查每一所**：对每一所消失或新增的学校都上网搜索，确认是否改名/转设/合并。候选列表给全部历史独有校（不只字面相似的）——真正的旧校名可能字面差异很大（如「佛山科学技术学院」→「佛山大学」），不要因字面不像就放过，必要时直接搜新校名的官方信息确认前身。

确认改名后，程序自动联动：
- 新校名 + 旧校名记入改名表。合并情形（一个新校名由多所旧校合并，如应急管理大学 = 华北科技学院 + 防灾科技学院）每个旧校名都记。
- 旧校名的往年线差数据并入新校名——改名的学校今年招的专业，在程序严格匹配 + agent 语义匹配阶段自动用旧校名的往年数据（不再 J/T 留空）。
- 旧校名从「停招消失校表」移出，新校名从「新增校表」移出（它们是改名的，不是真停招/真新增）。

网查结论 + 官方来源链接（教育部 moe.gov.cn 或省政府公文，**不认**第三方百科/新闻）填进 `rename_result.jsonl` 每行的 **`note`** 字段（is_rename=true 的每行都填）——它会直接进改名表备注列，是用户看到改名依据的唯一入口。网查详情另存 `research/<新校名>.md`（每校一个文件）备查。

### 5.3 被删的旧专业

往年有、今年该校还在但没招这个专业了 → 记录到被删旧专业表（保留往年的线差数据）。

### 5.4 新增的学校

今年有、往年完全没有在山东招生的学校 → 记录到新增校表。

### 5.5 停招消失的学校

往年有、今年完全没在山东招生的学校（排除已确认改名的）→ 记录到停招消失校表。

**完成判据**：每个大绿本本科专业都有明确归宿——匹配成功 / 估算 / 特殊情况 / 改名联动。

---

## 第六步：写出 8 张表 + 审计

### 输出文件

| 文件 | 内容 |
|------|------|
| 大绿本_完整版_含线差.xlsx | 完整大绿本副本（保留原始层级结构）+ 行尾追加线差、标准差、匹配方式、原因说明等列 |
| 大绿本_专业列表_含线差.xlsx | 只保留专业行（去掉批次标题、学校行）+ 同样的追加列 |
| 今年新增往年没有的专业.xlsx | 今年有、往年没有的新专业，带估算的线差和标准差 |
| 往年有但今年停招的专业.xlsx | 往年有、今年该专业停招的，带往年的线差数据 |
| 未能匹配的专业.xlsx | 对不上/复核存疑的专业。「对不上」的（多对一/类别冲突）带**估算线差+标准差**（同校同选科均值）+ 估算方式 + 用了几条往年数据 + 原因说明；飞行/表意特别模糊的估算列留空。 |
| 学校改名表.xlsx | 确认改名/转设/合并的学校，新校名↔旧校名 + 网查备注 |
| 今年新招生的学校.xlsx | 今年新出现在山东招生的学校 |
| 往年有今年停招的学校.xlsx | 往年有、今年消失的学校（排除已确认改名的） |

### 追加列说明（主表行尾，不覆盖原始数据）

1. **近三年统计线差**：匹配到的往年线差（J）；新增/未匹配的是估算值
2. **近三年线差标准差**：往年标准差（T）；仅一年数据的为空
3. **匹配方式**：严格匹配 / 核心名匹配 / agent 语义匹配 / 新增专业 / 新校无历史 / 未能匹配 —— 用来筛选
4. **仅一年数据**：往年只有一年数据（无标准差）标「是」
5. **选科要求跨年变化**：今年选科与往年不同标「是」（不影响匹配，仅提示）
6. **二次复核**：agent 语义匹配经复核「确定」的标「确定」（其余匹配方式为空——构造确定、免复核）
7. **原因说明**：用大白话写清楚为什么这么匹配、或者为什么没匹配上（不写术语，写人话）

### 审计

跑完管线后，必须跑审计脚本确认结果正确：

- 每个本科专业都有匹配方式标注（不能空）
- 匹配到的线差和标准差与往年源数据一致
- 没有全空的数据行
- 每张表都有数据
- 所有判断型匹配（agent 匹配的）都经过了二次复核

**审计 exit 0 才算完成。** pytest 全绿 ≠ 产出正确——必须对真实产出跑审计。

查任何产出/中间 xlsx 表用 `python -m scripts.show_table <file> [--head N] [--grep 词]`——不用每次现写 openpyxl 代码。

**完成判据**：审计 exit 0。

---

## 第七步：产出报告

生成 `output/数据整理报告.md`，给非技术用户看，**全程大白话，不写任何技术术语**（不要「Stage」「管线」「harness」「jsonl」「verify」「粗筛」等词）。

报告结构（按这个模板写，少自由发挥）：

1. **总览**：今年整理了 N 个本科专业。其中 X 个匹配上了往年录取数据（严格同名 M 个 + agent 语义判断 Y 个），Z 个是新专业（往年没有，估算了参考线差），W 个没匹配上（单独列表）。
2. **怎么读输出表**：逐张表说——主表（带线差的大绿本）、新专业表、没匹配上表、改名学校表、新增/停招学校表。每张表一句话说用途。
3. **线差怎么算的**：往年线差 = 录取最低分 − 一段线；新专业的估算线差 = 同校同类专业的均值。都保留 2 位小数。
4. **匹配方式列的值含义**：「严格匹配」=专业名归一化后完全一样；「核心名匹配」=往年同核心只 1 个、程序直接配；「agent 语义匹配」=专业名不同但 agent 判断是同一个（经二次复核）；「新增专业」=往年没这个专业，估了参考值；「新校无历史」=整所校近三年没在山东招过、留空；「未能匹配」=对不上（含复核存疑）。
5. **没匹配上的为什么**：挑典型例子说（如「某校的 X 专业，往年同校只有 Y 专业，方向不同对不上」）。

**完成判据**：报告文档产出（大白话，无术语）+ 审计 exit 0。pytest / ruff 是 plugin **开发时**的回归门（在源仓库跑），运行时的 venv 只装 openpyxl、不必跑它们——**审计 exit 0 是产出正确的唯一判据**。
