---
name: shandong-cee-line-diff
description: 山东高考录取数据整理——把今年的大绿本招生计划和往年的录取线差数据按专业语义匹配，给每个专业补上线差和标准差。Use when the user provides 山东高考/CEE admission xlsx and wants them matched. Annual reuse; asks data-setup questions first.
---

# 山东省高考录取数据匹配整理

把**今年的招生计划**（大绿本）里每个专业，和**往年的录取数据**按专业名语义匹配，给每个专业补上「往年线差」和「线差标准差」。匹配不上的单独列出，并标注原因。

匹配分两步：**程序严格匹配**（归一化后专业全名完全一致 = 直接匹配）+ **agent 语义匹配**（程序没匹配上的，由 agent 逐条读专业名、理解语义后判断能不能和往年某个专业配上）。agent 匹配完，再做一轮 **agent 二次复核**（逐条验证每个 agent 判断的匹配是否正确），确保结果准确。

---

## 第一步：问清楚数据在哪、怎么算

收到数据文件后（或用户提到山东高考/大绿本/线差），用 **AskUserQuestion** 逐批问以下问题。**用户在调用 prompt 里已经说了的就不问，只问没说的。** 能从文件内容看出来的（如年份、列名）也不问，直接读。

### 第 1 批（文件位置 + 一段线）

1. 今年的招生计划（大绿本）xlsx 文件在哪个路径？
2. 往年的录取数据 xlsx 文件在哪个路径？
3. 有没有提前批的录取数据？如果有，文件在哪？
4. 往年每年的「一段线」（本科录取控制分数线）分别是多少？（如果数据文件里已经写明了就不用问）

### 第 2 批（计算方式 + 精度）

5. 往年数据里有没有现成的「线差」和「线差标准差」？还是需要从录取分数自己算？
6. 如果需要自己算：线差 = 录取最低分 − 一段线，对吗？还是有别的算法？
7. 计算结果保留几位小数？（默认 2 位）
8. 输出表需要在原始数据后面加哪些列？（默认：线差、标准差、匹配结果、原因说明）

### 第 3 批（范围 + 特殊处理）

9. 只整理本科？还是也包含专科？只常规批？还是也包含提前批？
10. 没匹配上的专业怎么处理？（估算一个参考值 / 留空 / 单独成表 / 以上都要）
11. 有没有已知改名的学校？如果有请列出来。
12. 输出格式：保留原始大绿本的层级结构（批次标题、学校行）？还是只要扁平的专业行？

**问完才开始处理。不确定的继续问，不猜、不静默跳过。**

---

## 第二步：读取数据 + 程序严格匹配

### 2.1 读取三个源文件

- **大绿本**：今年招生计划，按批次/学校/专业分层。专业行 = 代号和名称都非空。
- **往年录取数据**：往年每个学校每个专业的录取分数、线差、标准差。
- **提前批录取数据**：提前批的录取最低分（用来现场算线差 = 最低分 − 一段线）。

三个文件读取前后都做 SHA256 校验，确保字节没被改过。专科行（小标题含「专科」）排除。

### 2.2 程序严格匹配

对大绿本的每个专业，取专业名做归一化（统一全角半角、去空白、剥掉体检/语种/学费等不影响专业身份的描述性括号），然后和往年数据里的专业名精确比对。**归一化后完全一致 = 匹配成功**，直接填入往年的线差和标准差。

这一步是确定性的，不需要 agent 介入。大约能匹配 58%。

**完成判据**：每个大绿本本科专业都被处理过（匹配成功或标记为「待 agent 匹配」）。

---

## 第三步：agent 语义匹配

程序严格匹配没配上的专业，交给 agent 逐条处理。

### 怎么做

1. 对每个没配上的专业，找出同一所学校、专业名相似的往年候选（核心名 = 去掉所有括号后的基础专业名，核心名相同的就是候选）。
2. **agent 逐条读**：今年这个专业叫什么、往年候选分别叫什么，**语义判断**是不是同一个专业。
3. 语义相同 → 匹配成功，填入对应候选的线差和标准差。
4. 语义不同 → 标记「无法匹配」。
5. 不确定 → 标记存疑，留到第四步问用户（但应该很少，绝大多数 agent 自己就能判断）。

### agent 怎么判断「同一个专业」

这是 agent 需要的领域知识，帮它判断，不是限制它的规则：

- **今年的专业名通常比往年详细很多**（大绿本会写子专业清单、培养模式、校区、国际交流等大段描述），往年通常只写一个简短的专业名。所以匹配的时候看核心专业名 + 主要方向就行，多余的大段描述不影响。
- **以下差异不影响「是不是同一个专业」**（agent 应该忽略）：标点符号不同、词语顺序不同、体检/身高/色盲等要求、学费、培养模式描述、章程引用、语种要求。
- **以下差异说明是不同的专业**（agent 应该判不匹配）：专业方向实质不同（如「投资学(量化投资)」≠「投资学」）、性别不同（男 ≠ 女）、合作方式不同（中外合作 ≠ 普通班）、独立招生的不同校区（如华北电力大学保定校区 ≠ 北京校区，但同一学校不同校区一起招生的不算）、招生类别不同（普通 ≠ 地方专项 ≠ 综合评价）。
- **去年这所学校某个核心专业名只招了 1 个的，今年不管怎么变都是同一个专业**（以去年只招「金融学」为例）：今年拆成几个方向（金融学(方向A)、金融学(方向B)）、改了方向措辞（去年「数学」、今年「数学(拔尖)」）、改了标点或词序、加减了括号内容——都是同一个专业，都和去年的那 1 个匹配。只有往年同核心名有多个的，才需要仔细比较方向是否一致。
- **校区不确定时** → 上网搜索该校招生章程，确认是独立招生还是同一学校不同校区。
- **学校改名** → 上网搜索所有消失/新增的学校，确认是否改名/转设/合并。

### 数据量大时

25474 个专业中大约有 5000-6000 个需要 agent 匹配。用 subagent 并行处理（每个 subagent 处理一批，约 20 个专业），可以同时派多个 subagent。**不惜 token，追求准确**：每个专业都要认真读、认真判断，不能偷懒跳过。

**完成判据**：每个待 agent 匹配的专业都有结果（匹配成功 + 对应候选 / 无法匹配 / 存疑）。

---

## 第四步：agent 二次复核

所有 agent 判断的匹配（程序严格匹配以外的全部），再做一轮独立复核：另一个 agent 逐条看「这个匹配对不对」，判「确认」或「存疑」。

- 确认 → 留在主表。
- 存疑 → 移到特殊情况表，不进主表。

### 复核时怎么判断

复核 agent 需要的领域知识和第三步匹配 agent 完全一致，重点是这条：

- **去年这所学校某个核心专业名只招了 1 个的，今年不管怎么变都是同一个专业**——去年只招了 1 个，今年不管改方向/改名/换措辞（标点、词序、加减括号内容），都是同一个专业，判「确认」。只有往年同核心名有多个的，才需要仔细比较方向是否一致。这条最常被漏掉，会导致大量本该「确认」的被误判「存疑」。
- **方向实质不同**（如「投资学(量化投资)」≠「投资学」）→ 判「存疑」。
- 不确定 → 判「存疑」，宁可保守。

**这一步是硬性的**：主表里不能有未经复核的判断型匹配。宁可保守（多移几条到特殊情况），也不能让错配留在主表。

**完成判据**：每个判断型匹配都有复核结果（确认 / 存疑）。

---

## 第五步：处理边界情况

### 5.1 无法匹配的专业 → 估算

往年完全没有这个专业（同一所学校没有同名专业）→ 用同一所学校、相同选科的其他专业的线差均值估算一个参考值，标注「估算」。

如果整所学校往年完全没有在山东招生 → 无法估算，线差和标准差留空，标注「新校，无历史数据」。

### 5.2 改名/转设/合并的学校 → 网查 + 联动

往年有、今年消失的学校 × 今年有、往年没有的学校 → 可能是改名了。对**每一所**这样的学校都上网搜索，确认是否改名/转设/合并。确认改名后：
- 把新校名和旧校名记录到改名表。
- 旧校名的往年数据并入新校名（改名的学校今年招的专业，用旧校名的往年线差）。
- 一个新校名可能对应多个旧校名（合并情况），全部记录。

### 5.3 被删的旧专业

往年有、今年该校还在但没招这个专业了 → 记录到被删旧专业表（保留往年的线差数据）。

### 5.4 新增的学校

今年有、往年完全没有在山东招生的学校 → 记录到新增校表。

### 5.5 停招消失的学校

往年有、今年完全没在山东招生的学校（排除已确认改名的）→ 记录到停招消失校表。

**完成判据**：每个大绿本本科专业都有明确归宿——匹配成功 / 估算 / 特殊情况 / 改名联动。

---

## 第六步：写出 8 张表 + 审计

### 输出文件

| 文件 | 内容 |
|------|------|
| 大绿本_附线差_分层版.xlsx | 完整大绿本副本（保留原始层级结构）+ 行尾追加线差、标准差、匹配阶段、原因说明等列 |
| 大绿本_附线差_扁平版.xlsx | 只保留专业行（去掉批次标题、学校行）+ 同样的追加列 |
| 新增专业.xlsx | 今年有、往年没有的新专业，带估算的线差和标准差 |
| 被删旧专业.xlsx | 往年有、今年该专业停招的，带往年的线差数据 |
| 特殊情况.xlsx | 无法匹配或复核存疑的专业，带原因说明 |
| 学校改名表.xlsx | 确认改名/转设/合并的学校，新校名↔旧校名 + 网查备注 |
| 新增校表.xlsx | 今年新出现在山东招生的学校 |
| 停招消失校表.xlsx | 往年有、今年消失的学校（排除已确认改名的） |

### 追加列说明（主表行尾，不覆盖原始数据）

1. **往年线差**：匹配到的专业的往年线差（估算的标注「估算」）
2. **往年线差标准差**：同上
3. **匹配方式**：严格匹配 / agent 语义匹配 / 新增专业估算 / 特殊情况 / 改名 / 新校无历史 —— 用来筛选
4. **原因说明**：用大白话写清楚为什么这么匹配、或者为什么没匹配上（不写术语，写人话）

### 审计

跑完管线后，必须跑审计脚本确认结果正确：

- 每个本科专业都有匹配方式标注（不能空）
- 匹配到的线差和标准差与往年源数据一致
- 没有全空的数据行
- 每张表都有数据
- 所有判断型匹配（agent 匹配的）都经过了二次复核

**审计 exit 0 才算完成。** pytest 全绿 ≠ 产出正确——必须对真实产出跑审计。

**完成判据**：审计 exit 0。

---

## 第七步：产出报告

生成 `output/数据整理报告.md`，用大白话讲清楚：
- 整理了多少个专业，匹配成功多少、估算多少、特殊情况多少
- 每张表是什么、怎么用
- 线差和标准差怎么算的、保留几位小数
- 匹配方式列每个值的含义
- 没匹配上的为什么没匹配上

**完成判据**：报告文档产出 + 审计 exit 0 + pytest 全绿 + ruff 无错。
