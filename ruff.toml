# Linter config — reproducible "无 linter 错误" gate.
# Run: .venv/bin/ruff check scripts tests
target-version = "py312"

[lint]
select = ["E4", "E7", "E9", "F"]  # ruff defaults: pyflakes + core pycodestyle errors
