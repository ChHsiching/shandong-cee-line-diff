# data/ — 源数据（用户自行放入）

把山东省高考的三个原始 xlsx 放到本目录，文件名保持不变（若改名则同步改 `scripts/constants.py` 的列索引与 `run_pipeline.SOURCE_FILES`）：

| 文件 | 内容 |
|------|------|
| `山东省2026年大绿本招生计划.xlsx` | 当年招生计划（大绿本，主键源，分层结构） |
| `近三年学校批次专业线差统计.xlsx` | 近三年 学校/批次/专业 线差统计（常规批 J/T 源） |
| `山东省高考提前批录取数据.xlsx` | 提前批原始录取数据（提前批 J/T 计算源） |

> **这些文件含原始数据，永不提交。** `.gitignore` 已排除 `data/*`，仅本 README 入库；
> 仓库分发与安装器克隆均**不含任何数据文件**。
